# secrets — never commit
.env
.env.local
.env.*.local

# deps & build
node_modules/
.next/
dist/
out/
coverage/
*.tsbuildinfo
next-env.d.ts
.vercel/

# circle wallet recovery files (Phase 3) — never commit
circle-recovery.dat
recovery_file_*.dat

# runtime data store (agents, predictions) — local, not committed
.data/

# foundry
contracts/out/
contracts/cache/
contracts/broadcast/

# misc
.DS_Store
*.log
