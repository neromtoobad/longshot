{
  "name": "@longshot/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@longshot/shared": "workspace:*"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src"
  }
}
