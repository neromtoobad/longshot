{
  "name": "@longshot/agent",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "dependencies": {
    "@circle-fin/developer-controlled-wallets": "^10.0.0",
    "@longshot/shared": "workspace:*",
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@types/node": "^22.10.2",
    "tsx": "^4.21.0"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json --noEmit",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "lint": "eslint src",
    "circle:register": "tsx --env-file=../.env scripts/register-entity-secret.ts",
    "wallet:demo": "tsx --env-file=../.env scripts/wallet-demo.ts",
    "pay:demo": "tsx --env-file=../.env scripts/pay-demo.ts"
  }
}
