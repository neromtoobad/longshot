[profile.default]
src = "src"
out = "out"
test = "test"
script = "script"
libs = ["lib"]
solc = "0.8.24"
optimizer = true
optimizer_runs = 200

# Arc testnet. RPC is supplied at runtime via $RPC (arc-canteen). Do not hardcode it here.
# Phase 2 adds AgentRegistry.sol + Pool.sol and installs forge-std for tests.
